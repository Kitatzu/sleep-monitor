{
  "name": "sleep-monitor",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9",
    "lint-staged": "^15",
    "prettier": "^3",
    "prettier-plugin-astro": "^0.14"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": "prettier --write",
    "**/*.astro": "prettier --write",
    "**/*.go": "gofmt -w"
  }
}
